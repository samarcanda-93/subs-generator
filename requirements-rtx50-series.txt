# Requirements for RTX 50 series GPUs (CUDA compute capability sm_120)
# Install base requirements first: pip install -r requirements-base.txt
# Then install PyTorch nightly with CUDA support:

# PyTorch nightly build (required for RTX 50 series support)
# Install with:
# pip install torch --pre --index-url https://download.pytorch.org/whl/nightly/cu124

# Note: You will also need cuDNN 9.x files in your project directory
# Download from NVIDIA Developer website and place the .dll files in your project root